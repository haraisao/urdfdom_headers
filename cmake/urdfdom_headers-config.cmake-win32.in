if (@PACKAGE_NAME@_CONFIG_INCLUDED)
  return()
endif()
set(@PACKAGE_NAME@_CONFIG_INCLUDED TRUE)

if(DEFINED ENV{ROS_HOME_CMAKE})
  set(ROS_HOME $ENV{ROS_HOME_CMAKE})
else()
  set(ROS_HOME "@CMAKE_INSTALL_PREFIX@")
endif()

set(@PACKAGE_NAME@_INCLUDE_DIRS "${ROS_HOME}/include")
